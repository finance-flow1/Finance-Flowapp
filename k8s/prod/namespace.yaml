# =============================================================================
#  Namespace — prod
# =============================================================================
apiVersion: v1
kind: Namespace
metadata:
  name: prod
  labels:
    app.kubernetes.io/part-of: finance-app
    environment: prod
    kubernetes.io/metadata.name: prod
