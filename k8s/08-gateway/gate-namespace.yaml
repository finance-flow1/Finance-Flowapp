# =============================================================================
#  Namespace — gate (cluster-level ingress namespace for kgateway)
#  Applied once as part of gateway infrastructure setup.
# =============================================================================
apiVersion: v1
kind: Namespace
metadata:
  name: gate
  labels:
    app.kubernetes.io/part-of: finance-app
    purpose: ingress
    kubernetes.io/metadata.name: gate
